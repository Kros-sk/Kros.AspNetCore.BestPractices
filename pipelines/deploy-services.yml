steps:
  - template: deploy-microservice.yml
    parameters:
      microservices: ['ToDos', 'Authorization', 'Organizations']

  - template: deploy-apigateway.yml
    parameters:
      apiGatewayName: 'ApiGateway'